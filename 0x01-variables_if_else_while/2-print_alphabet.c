#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	for (char letter = 'a'; letter <= 'z'; letter++)
		putchar(letter);
		printf("\n");

	return (0);
}
