#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0. Success
 */

int main(void)
{
	int value;

	for (value = 0; value < 10; value++)
	{
		putchar(x + '0');

		if (value < 9)
		{
			putchar(',');
			putchar(' ');
		}
	}

	putchar('\n');
	return (0);
}
